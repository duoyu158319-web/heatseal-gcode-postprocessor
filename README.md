# Heat-seal G-code postprocessor

本地运行的 Bambu Studio `.gcode.3mf` 后处理网页。它只在文件已有的 Bambu 暂停层之后，根据上一层识别出的闭合轨迹生成无挤出热封路径，不会新增暂停。

主要功能：

- 即使模型已经合并为同一主体，也会按几何嵌套关系区分中间主体骨架与热封外圈。
- 只有包含 `M400 U1` 的 Bambu 已有暂停层可以选择，不默认选择最高层。
- 自动确定复走圈数，避免选择超过识别结果。
- 最外圈可选择热封 1 遍或 2 遍，其余轨迹保持 1 遍。
- 为每一圈独立设置相对速度，并以 `mm/s` 显示实际速度。
- 为每一圈独立设置 `0–0.5 mm` 外扩量，步长 `0.1 mm`，默认 `0.2 mm`。
- 使用圆角等距外偏，处理直线和 `G2/G3` 圆弧。
- 导出重新封装且更新 MD5 的 `.gcode.3mf` 文件。

## 本地运行

```powershell
node web/server.mjs
```

打开 `http://127.0.0.1:4173/`。

## 测试

```powershell
node --test web/core.test.mjs
```

## 安全提示

G-code 后处理涉及真实机械运动。导出前请检查暂停层、复走高度、速度、外扩方向和路径预览；首次运行新参数时应全程观察并准备停止打印。
